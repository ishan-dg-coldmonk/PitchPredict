[build]
  command = "npm run build"
  publish = "dist"

# Vite 8 requires a modern Node — Netlify's default is too old and the build fails.
[build.environment]
  NODE_VERSION = "22"

# SPA fallback — all other routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
