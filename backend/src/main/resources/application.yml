spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:pitchpredict-super-secret-key-that-is-256-bits-long-for-hmac-sha512}
  expiration: 86400000

football-data:
  api-key: ${FOOTBALL_DATA_API_KEY:}
  base-url: https://api.football-data.org/v4
